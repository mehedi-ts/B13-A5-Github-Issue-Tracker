## What is the difference between var, let, and const?
### ans: var is a function scoped variable ,can be reassigned, let is a block-scoped variable,can ve reassigned , const is a block-scoped variable, cnanot be reassigned.

## What is the spread operator (...)?
### ans: it's used to copies or expands arrays/objects

## What is the difference between map(), filter(), and forEach()?

### ans: 
- map() returns a array, transforms elements
- filter() returns a new array,keeps elements matching condition.
- forEach() loops through elements, does not return anything

## What is an arrow function?

### ans: arrow function is a short syntax for writing functions using "()=>".

## What are template literals?

### ans: template literals are string written with bacticks ` that allow embedding bariables and expressions using ${}